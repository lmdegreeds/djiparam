# Normalize line endings; keep shell scripts LF so they run on the RC / Git Bash.
* text=auto
*.sh   text eol=lf
*.py   text eol=lf
*.java text eol=lf
*.ps1  text eol=crlf

# Treat binaries as-is
*.png  binary
*.apk  binary
*.keystore binary
